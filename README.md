# ShiftPay Calc

US hourly-pay toolkit: no tax on overtime, no tax on tips, night-shift differential, California overtime, timesheet + lunch.

Client-side Next.js. No backend. Estimates only.

```bash
npm test
npm run dev
npm run build
```

Formulas and sources: [src/app/methodology/page.tsx](src/app/methodology/page.tsx).
